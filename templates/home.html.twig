{% extends 'base.html.twig' %}
{% block title %}
Appartement et location
{% endblock %}

{% block header %}
<h2>Voici le header de la page home</h2>
{% endblock %}

{% block body %}

<div class="container">
    <div class="jumbotron my-4">
        <h1 class="display-4">Bienvenue sur SymBnB</h1>
        <p class="lead">Nous mettons en relation les voyageurs et les propriétaies qui veulent profiter d'opportunités et gagner de l'argent. En proposant leur bien à la location</p>
        <hr class="my-4">
        <p>Vous êtes voyageurs et vous voulez voir ou vous pouvez loger pour votre prochaine déplacement ?</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="{{path('ads_index')}}" role="button">Voir nos annonces.</a>
        </p>
    </div>
    <h1 class="my-4">Nos appartements stars</h1>
    <div class="row">
        {% for data in ads %}
            {% include 'ad/_ad.html.twig' with {'ad':data.annonce}%}
        {% endfor %}
    </div>
    <h1 class="my-4">Nos propriétaires stars</h1>
    <div class="row">
        {% for data in users %}
        <div class="col-md-6">
            <div class="card border-secondary mb-3">
                <div class="card-body">
                    <h4 class="card-title">{{data.user.fullName}}</h4>
                    <hr>
                    <div class="card-text">
                    <img src="{{data.user.picture}}" alt="Avatar de {{data.user.fullName}}" class="float-left mr-3">
                    <p>{{data.user.introduction}}</p>
                    <a href="{{ path('user_show', {'slug':data.user.slug})}}" class="btn btn-success float-right"> Voulez vous en savoir plus ?</a>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
